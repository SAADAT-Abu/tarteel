# ── Copy this file to /opt/tarteel/.env on the server ─────────────────
# Fill in every value marked CHANGE_ME before first deploy.

# ── Database ──────────────────────────────────────────────────────────
DB_PASSWORD=CHANGE_ME_strong_db_password
DATABASE_URL=postgresql+asyncpg://tarteel:CHANGE_ME_strong_db_password@db:5432/tarteel

# ── Redis ─────────────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── JWT (generate: openssl rand -hex 32) ──────────────────────────────
JWT_SECRET_KEY=CHANGE_ME_run_openssl_rand_hex_32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# ── Admin API ─────────────────────────────────────────────────────────
ADMIN_API_KEY=CHANGE_ME_run_openssl_rand_hex_32

# ── Cookies ───────────────────────────────────────────────────────────
COOKIE_SECURE=true

# ── URLs ──────────────────────────────────────────────────────────────
FRONTEND_URL=https://tarteel.live
BACKEND_URL=https://api.tarteel.live
HLS_SERVE_URL=https://api.tarteel.live

# ── Audio paths (inside Docker container) ─────────────────────────────
AUDIO_DIR=/app/audio
HLS_OUTPUT_DIR=/app/hls
DEFAULT_RECITER=Alafasy_128kbps

# ── Ramadan ───────────────────────────────────────────────────────────
RAMADAN_START_DATE=2026-02-18
RAMADAN_TOTAL_NIGHTS=30

# ── Email — Brevo SMTP (primary, recommended) ─────────────────────────
BREVO_SMTP_USER=CHANGE_ME_your_brevo_login_email
BREVO_SMTP_KEY=CHANGE_ME_your_brevo_smtp_key

# ── Email — SendGrid (alternative to Brevo) ───────────────────────────
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@tarteel.live

# ── Email — Gmail SMTP (alternative, good for testing) ────────────────
GMAIL_USER=
GMAIL_APP_PASSWORD=

# ── WhatsApp — Twilio (optional) ──────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# ── Geocoding — OpenCage (optional, falls back to Nominatim) ──────────
OPENCAGE_API_KEY=

# ── Frontend (baked into Next.js bundle at build time) ────────────────
NEXT_PUBLIC_API_URL=https://api.tarteel.live
NEXT_PUBLIC_WS_URL=https://api.tarteel.live
